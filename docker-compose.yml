version: '3.4'

services:
  hellolemoncoder:
    image: hellolemoncoder
    build:
      context: reto5/hello-bootcamp
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 4000:4000
